<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainActivity"
    tools:showIn="@layout/activity_main">

    <Button
        android:id="@+id/startServer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="StartServer"
        tools:layout_editor_absoluteX="146dp"
        tools:layout_editor_absoluteY="57dp" />

    <Button
        android:id="@+id/searchServer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="SearchServer"
        app:layout_constraintTop_toBottomOf="@+id/stopServer"
        tools:layout_editor_absoluteX="141dp" />

    <TextView
        android:id="@+id/ipAddress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="64dp"
        app:layout_constraintTop_toTopOf="@id/searchServer"
        tools:layout_editor_absoluteX="205dp" />

    <Button
        android:id="@+id/stopServer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="stopServer"
        app:layout_constraintTop_toBottomOf="@+id/startServer"
        tools:layout_editor_absoluteX="150dp" />

</androidx.constraintlayout.widget.ConstraintLayout>